pipeline {
  agent any
  stages {
    stage('build') {
      steps {
        sh './mvnw clean package -Dcheckstyle.skip'
      }
    }
    stage('Generate SBOM') {
      steps {
        sh 'syft scan dir:. -o spdx-json@2.3  > spring-petclinic.spdx.json'
      }
    }
  }
}
